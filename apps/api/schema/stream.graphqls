extend type Query {
    stream: Stream
}

extend type Subscription {
    streamInfo(channelID: UUID!): Stream
}

type Stream {
    viewers: Int!
    chatters: [Chatter!]!
    startedAt: Time
}

type Chatter {
    user: User!
}