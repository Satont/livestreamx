extend type Query {
    streams: [Stream!]!
}

extend type Subscription {
    streamInfo(channelID: UUID!): Stream
}

type Stream {
    viewers: Int!
    chatters: [Chatter!]!
    startedAt: Time
    channel: User!
}

type Chatter {
    user: User!
}